import type { SchemaTypeDefinition } from "sanity";

/**
 * Week 0 keeps this empty on purpose: no content schema is approved yet.
 * Week 2 defines the real schema system here.
 */
export const schemaTypes: SchemaTypeDefinition[] = [];
