import { homepageFallbackContent } from "@/lib/content/homepage";

/**
 * RW-PHASE-02: verbatim reuse of the homepage's platform-parity content —
 * real AWS/Microsoft group items and the shared-layer list, already written
 * and already condensed once for the homepage split section. Nothing here
 * is new.
 */
export const platformsContent = homepageFallbackContent.platformParity;
