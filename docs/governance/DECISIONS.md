# Decision Log

Record only material decisions that affect implementation, authority, scope or release.

| ID | Date | Decision | Authority | State | Repository effect |
|---|---|---|---|---|---|
| GOV-001 | 2026-07-30 | Claude defines/reviews; Codex implements/verifies; only one implementation writer owns a work package at a time. | Silvester | `APPROVED` | Enforced by root `AGENTS.md` |
| GOV-002 | 2026-07-30 | Repository evidence, not an agent report alone, determines implementation state. | Silvester | `APPROVED` | Commit/branch/test evidence required |
| GOV-003 | 2026-07-30 | Silvester decides objectives and visual acceptance; Trueman owns architecture/protected production authority; Miles approves public proof. | v40.0 playbook + Silvester | `APPROVED` | Authority matrix created |
| GOV-004 | 2026-07-30 | Week 1 must be accepted and merged to `develop` before Codex begins Week 2. | Silvester governance transition | `APPROVED` | Transition checklist in execution state |
| GOV-005 | 2026-07-30 | Sanity `production` remains read-only until Trueman authorizes a specific write. | v40.0 playbook / Trueman boundary | `APPROVED` | All agents must stop before production writes |
| GOV-006 | 2026-07-31 | Week 1 visual refinement (commit `20efe31`) is visually accepted — explicit conversational approval, "Outcome B — Visual refinement approved." | Silvester | `APPROVED` | `20efe31` pushed to `origin/feature/week-1-design-shell-homepage`; `EXECUTION_STATE.md` Week 1 visual refinement state raised to `VERIFIED`; merge into `develop` still requires governance adoption first (GOV-004) plus a separate Week 1 PR |
| GOV-008 | 2026-08-01 | Official 21st.dev MCP (`https://21st.dev/api/mcp`) approved for Rive use, authenticated via `${API_KEY_21ST}` with no literal key committed; unofficial substitutes, ports or repackaged alternatives are prohibited for this capability, and passive connection does not satisfy the invocation requirement — governed tasks must show genuine `mcp__21st__*` tool invocation. TasteSkill remains `AWAITING_OFFICIAL_MCP`: no official MCP connection has been provided, so the capability is not approved for Rive execution and tasks genuinely requiring it must stop at the Skill Gate. `GOV-007` is not active or authoritative for this baseline — it exists only on the unmerged, quarantined `chore/register-taste-skill-capability` branch and has not been accepted into `develop`; this entry intentionally continues the sequence at `GOV-008` rather than reuse or renumber it. | Product Office (`RIVE-PO-012`, `RIVE-PO-014`, `RIVE-PO-015`, `RIVE-PO-016`) | `APPROVED` | `.mcp.json` added (env-var auth only, no secret); `SKILL_REGISTRY.md` updated with 21st.dev `OFFICIAL_MCP_CONNECTED_CALLABLE` classification and TasteSkill `AWAITING_OFFICIAL_MCP` classification |
| GOV-009 | 2026-08-03 | Retired the "multiple chat window offices" model. The Product Office chat is now the only long-lived strategic conversation and no longer stores long-term project memory; repository-resident offices under `docs/` (indexed at `docs/OFFICES.md`) are authoritative for creative direction, UX, design system, engineering, QA, research and architecture, each with a short charter that links to the existing `docs/governance/` process layer instead of duplicating it. Engineering must read the relevant office before implementing — repository first, conversation second — and chat history is never project memory. `AGENTS.md` gained an explicit "Git authority" section: task completion, implementation and verification never authorize `git add`/`commit`/`push`/`merge`/`tag`; only the user decides. This was a documentation-only refactor — no functional website or source code was changed, and no git write operation was performed as part of it. | Product Office | `APPROVED` | Added: `docs/OFFICES.md`; `docs/product-office/00-CHARTER.md`; `docs/creative-direction/00-CHARTER.md` and `VISUAL_LANGUAGE.md`; `docs/ux/00-CHARTER.md`; `docs/design-system/00-CHARTER.md`; `docs/engineering/00-CHARTER.md`; `docs/qa/00-CHARTER.md`; `docs/research/00-CHARTER.md`; `docs/architecture/00-CHARTER.md`. Modified: `AGENTS.md` (new "Repository-resident offices" and "Git authority" sections); `docs/governance/README.md` (pointer to office folders). No change to `docs/governance/DECISIONS.md`, `EXECUTION_STATE.md`, `AUTHORITY.md` or `CLAIMS_REGISTER.md` structure — those remain the single, non-duplicated logs. |

| GOV-010 | 2026-08-03 | Adopted the Capability Governance Framework (CGF v1.0): every external capability Rive uses or considers must be recorded in `docs/capabilities/CAPABILITY_REGISTER.md` with an evidence-based classification (`VERIFIED`, `CONNECTED`, `EXPERIMENTAL`, `PLANNED`, `RETIRED`), distinct from the per-work-package Skill Gate vocabulary in `SKILL_REGISTRY.md` §5. Engineering may only rely on `VERIFIED` capabilities, or `CONNECTED` ones with explicit acknowledged risk; `EXPERIMENTAL`/`PLANNED` can never be a mandatory dependency. Also created the Human Design Review capability (`docs/capabilities/HUMAN_DESIGN_REVIEW.md`): a measurable checklist to reject AI-generated design mediocrity ("AI slop") before Product Office acceptance — it rejects, it does not generate, and it does not replace Silvester Odilu's visual acceptance authority. Initial audit performed with genuine same-session evidence, not assumption: 21st.dev MCP → `VERIFIED` (live `mcp__21st__get_usage`/`list_teams` calls returned real data); UI/UX Pro Max → `VERIFIED` (live `search.py` invocation via the installed plugin returned real database results); Taste Skill → `PLANNED` (confirmed genuinely absent — no plugin, no MCP, no file evidence anywhere in this environment; consistent with, not a change from, `GOV-008`'s `AWAITING_OFFICIAL_MCP`); Figma MCP → `CONNECTED` as a bonus finding (present via the operator's global claude.ai connector, not pinned in this repo's `.mcp.json`, unvalidated for Rive use). This was a documentation-only refactor — no functional website or source code was changed, and no git write operation was performed, per the directive's explicit Git Authority instruction. | Product Office | `APPROVED` | Added: `docs/capabilities/00-CHARTER.md`, `CAPABILITY_REGISTER.md`, `HUMAN_DESIGN_REVIEW.md`. Modified: `AGENTS.md` (new "Capability governance" section); `docs/OFFICES.md` (Capabilities office + two routing rows); `docs/governance/SKILL_REGISTRY.md` (§5 cross-reference note + dated changelog row). |

| GOV-011 | 2026-08-03 | Product Office directed reactivation of the quarantined `Leonxlnx/taste-skill` third-party package (`GOV-007`), reversing `GOV-008`'s non-authoritative treatment for this specific action. Content was restored from commit `56da308` (`chore/register-taste-skill-capability`, still unmerged into `develop`/`main`) into the working tree: all 13 skills' `SKILL.md`/`DESIGN.md` content under `.agents/skills/*` plus `skills-lock.json`, verified byte-for-byte from that commit. Reactivation is **partial, not complete**: the `.claude/skills/*` symlinks that make these skills actually invocable by Claude Code were not created — the environment's own auto-mode safety classifier denied both `mkdir .claude/skills` and `ln -s` into it, twice, independent of any project git rule (this is a platform-level guard against an agent silently installing new skills for itself, not a git-authority or governance restriction). Nothing was staged or committed. Reconciliation flagged in the original audit remains open and unresolved: overlap between several of these skills (`gpt-taste`, `high-end-visual-design`, `minimalist-ui`, `industrial-brutalist-ui`, `imagegen-frontend-web`/`mobile`, `redesign-existing-projects`) and `UI/UX Pro Max` (already `VERIFIED` in `CAPABILITY_REGISTER.md`), compatibility with `docs/capabilities/HUMAN_DESIGN_REVIEW.md`, and the third-party/unofficial provenance noted in `GOV-007` (MIT-licensed, individual-maintainer GitHub repo, not a vendor-official source). | Product Office | `APPROVED` (decision to reactivate); repository effect `IMPLEMENTED` for content restoration, `BLOCKED` for symlink activation | `.agents/skills/*` (14 files) and `skills-lock.json` restored to the working tree, untracked; `.claude/skills/*` not created; `docs/capabilities/CAPABILITY_REGISTER.md` and `docs/governance/SKILL_REGISTRY.md` updated to reflect the partial state |

| GOV-012 | 2026-08-04 | RW-PW07B (Calm Header, Consolidated Navigation & Visual Mega Menus) approved and implemented, correcting RW-PW07A/RW-PW07's "preserve all eight visible items" instruction. Reduced top-level nav from 8 to 5 (Solutions/Services/Work/Resources/Company); Industries, Platforms, Pricing and the Trust group are consolidated into mega-menu `secondaryLinks` rather than dropped or moved to a separate desktop drawer — every prior route remains reachable, verified by a dedicated unit test (`no route from the old 8-item model is orphaned`), so per the work package's own §5 ("where routes cannot be grouped naturally, implement a drawer") no drawer was built, since grouping alone achieved full coverage. Mega-menu panel rebuilt from a 560px per-trigger dropdown into a full-width floating card matching the operator-supplied reference recording: gradient "featured" tile + secondary-link rail on the left, icon+label+arrow tile grid with the mandated `Placeholder` visual on the right. Button system rebuilt (`src/components/ui/Button.tsx`) into six variants (primary/secondary/inverse/text/nav/quiet) with a solid left-to-right colour-fill hover (no lift, no hover shadow — corrected mid-session after the first pass used lift+shadow, per direct operator feedback) and a few-pixel arrow nudge, reduced-motion-safe throughout. | Product Office / Silvester (live, in-session direction, including two screenshots of a reference mega-menu and an explicit correction of the button hover treatment) | `APPROVED` and `IMPLEMENTED`, technically `VERIFIED` (lint/typecheck/23 unit tests/build/54 e2e incl. axe both themes all passing); visual review completed live at 1440×900 only (desktop mega menus ×5, button hover fill, sticky state, light+dark theme) — the required 390×844/768×1024/1024×768/1280×800/1366×768 viewports could not be visually captured this session (browser window-resize tool did not take effect against the shared, already-open Chrome window across three attempts on two tabs; see EXECUTION_STATE.md for detail), so responsive correctness at those sizes rests on the automated Playwright viewport suite (`mobile-nav.spec.ts` at 390×844), not a human/agent screenshot | `src/lib/nav.ts`, `src/components/nav/{Header,DesktopNav,MegaMenuPanel,MobileNav}.tsx`, `src/components/ui/Button.tsx` and its ~10 call sites, `tests/unit/nav.test.ts`, `tests/e2e/{navigation,mobile-nav,industries-platforms}.spec.ts` all modified; working tree left unstaged per Git Authority |

| GOV-013 | 2026-08-05 | RW-PW08 (Transform the Capability Explorer into a Signature Experience) approved and implemented. Redesigned `OutcomeExplorer` (the homepage's "Tell us what's broken" section, `BuyerPathGrid`) from a functional tab/panel into an editorial workspace: per-capability line icons + a sliding beam indicator replace the plain filled-circle tablist; the numbered badge moved from an isolated floating circle to an inline "editorial stamp" beside the headline; a new bespoke Visual Story Panel per capability (`CapabilityVisual.tsx`, 4 distinct compositions — architecture/topology/node-network/relationship — each carrying full placeholder metadata incl. asset ID, aspect 16:10, motion intent) is now the section's visual hero; symptoms + the examine/output/follow-on detail moved behind a "See how we work" progressive-disclosure toggle so the section leads with demonstration, not documentation; the right panel is `lg:sticky` (operator-reported bug: it wasn't pinning while scrolling — fixed same session). Mobile (`BuyerPathAccordion`) got the same icon + Visual Story Panel per item, kept as a native-`<details>` list otherwise (full parallel mobile rebuild was out of scope). Separately, default theme changed from "system preference" to always-light on first visit (explicit operator instruction) — dark remains an explicit, persisted opt-in via the toggle. | Product Office / Silvester (live, in-session direction) | `APPROVED` and `IMPLEMENTED`, technically `VERIFIED` (lint/typecheck/23 unit tests/build/54 e2e incl. axe both themes all passing); visually `VERIFIED` live at 1440×900 (all 4 capabilities, sliding beam, progressive disclosure, sticky panel, theme-default fix) — the other 5 required viewports were not captured this session, same tooling constraint recorded under `GOV-012` | Added `src/components/homepage/CapabilityVisual.tsx`; modified `src/components/homepage/OutcomeExplorer.tsx`, `src/components/homepage/BuyerPathAccordion.tsx`, `src/app/layout.tsx`; also fixed a genuine contrast regression introduced earlier this session in `GovernedAISection.tsx`/`ProcessStepper.tsx` (gold-deep/azure-strong text on dark surfaces — the codebase's own token convention reserves those for borders/light-surface use, not dark-surface text); working tree left unstaged per Git Authority |

| GOV-014 | 2026-08-12 | RW-GOV-04B restores the full major-visual capability pipeline. UI/UX Pro Max is a required package-specific reasoning pass; 21st.dev/Magic is a required purposeful exploration pass rather than a required component import; TasteSkill is the critic rather than the primary generator; Rive pages are continuity references rather than substitutes for exploration; a Design Decision Brief and cross-page repetition audit precede implementation; Human Design Review, responsive QA, Playwright, axe/WCAG and Silvester acceptance follow it. Durable capability state and current-session callability must be reported separately. Failed required invocations must be diagnosed and escalated; only Product Office may grant `PROCEED WITH EXCEPTION` for one named package. Homepage, Work, Guides, Insights and About remain accepted build-phase implementations and enter RW-REFINE-01 full-capability refinement debt; they are not rejected or rebuilt by this decision. | Product Office / Silvester (RW-GOV-04B directive) | `APPROVED` and governance `IMPLEMENTED` | Active rules encoded in `CLAUDE.md`, `SKILL_REGISTRY.md`, capability evidence, Human Design Review and governance templates; historical reports preserved |
| GOV-015 | 2026-08-12 | Audit and review findings are recommendations, not authority to change product scope. Findings progress through `OBSERVATION`, `RECOMMENDATION`, `PRODUCT OFFICE ACCEPTED`, `AUTHORISED FOR IMPLEMENTATION`, and `IMPLEMENTED`; only the final two states authorise implementation. Unbuilt destinations remain planned build work unless Product Office explicitly removes them, accepts an obsolete-route recommendation, or approves a replacement. RW-IA-01's navigation/footer/CTA reduction is rejected and superseded; the four-layer audit itself remains accepted. | Product Office / Silvester (RW-RECOVERY-01) | `APPROVED` | RW-IA-01 implementation reverted non-destructively; planned pre-IA public architecture restored; Process, RW-FIX-02 and the audit preserved |
| GOV-016 | 2026-08-13 | `/work/` is the canonical font-size reference for equivalent typography roles across the Rive website. Page hero H1 and equivalent hero-lead scales are normalized to Work while page-specific measure, alignment, wrapping, composition and semantically distinct editorial or interactive display roles remain independent. | Product Office / Silvester (`RW-TYPE-01`) | `APPROVED`; implementation `IN_PROGRESS` | Design-system typography guidance recorded in `docs/design-system/TYPOGRAPHY.md`; implementation assigned to `feature/sitewide-typography-normalisation` with no navigation, content or layout-redesign authority. |
| GOV-017 | 2026-08-13 | Adopted the Rive Authored Page Standard for the lifespan of the current website development. Industries establishes the Product Office quality benchmark but is not a compositional template. Every future major visual page requires a page-specific visual thesis, section rhythm, Media Opportunity Audit and approximately one or two memorable moments before implementation. Human Design Review adds full-page screenshot, squint, template-resistance and content-swap gates. Existing accepted pages remain accepted; the standard applies when future major pages or separately commissioned refinements are designed. | Product Office / Silvester (`RW-GOV-05`) | `APPROVED` and governance `IMPLEMENTED` | Creative Direction owns the doctrine; Human Design Review owns qualitative enforcement; Product Office owns acceptance, exceptions and supersession. Major-visual workflow and work-package/evidence templates updated without application, route or product-scope changes. |

## Entry format

Add one row with:

- immutable ID;
- decision date;
- precise decision;
- named human authority;
- approved vocabulary state;
- concrete repository or workflow effect.

Do not rewrite past decisions. Add a superseding decision that cites the earlier ID.
